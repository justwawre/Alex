# packing data outside of struct
e.g.
 change flag(U16) int to bit, then using ULMACCE_U16_GET_BIT_VALUE()

# now c99 support
* anonymous union: so that 2 data can share in time domain
* mix signed/unsigned bits field in


# code refactor 
/*
时间复杂度低于 O(n*k); n: num of se ; k: num of cell
*/

validate_goldentrace()
{
    //CONST  MAX_SE, MAX_CELL
    array[MAX_SE] 
    copy all validator's CM output to array, reacord the TOTAL_NUM_SE;
    and get count_of_se;

    int start = 0;
    int count = 0;
    //traverse the array
    while (count < count_of_se)
    {
        buffer[max_se_per_cell];//define the mesasge is part of signal 
        count_se_cell = 0;
        read from the start, 
        get its cellid
        count ++;
        copy its data to buffer
        bFirstDifferentCell = true;
        for (int i = start +1; i<count_of_se && count < count_of_se ;i++)
        {
            if the （new cell id of i ） == cell id
            {
                count ++;
                copy its data to buffer[count_se_cell++]
                
            }
            else if (bFirstDifferentCell)
            {
                start = i;
                bFirstDifferentCell = false;
            }
            else{
                //do nothing
            }//if-else
        }//for 
        copy buffer to CM,
        send the signal
    }//while
}
